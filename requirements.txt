# kor_yt_md_pipeline.py / tech_yt_md_pipeline.py / monitor_server.py
# (_vault_setup\01_md_convert\_scripts\) 실행에 필요한 패키지
# 설치: py -3.12 -m venv .venv  →  .venv\Scripts\pip install -r requirements.txt
# ffmpeg는 pip 패키지가 아니므로 별도로 PATH에 설치돼 있어야 함 (winget 등으로 이미 설치돼 있음)

openpyxl
python-dotenv
yt-dlp
pillow
google-genai

# Whisper 받아쓰기 (faster-whisper, GPU: RTX 4070)
faster-whisper
# ctranslate2가 Windows에서 시스템 CUDA 툴킷 없이 GPU를 쓰기 위해 필요
# (kor_yt_md_pipeline.py의 _register_nvidia_dlls()가 site-packages\nvidia\*\bin 을 PATH/DLL 검색경로에 자동 등록함)
nvidia-cublas-cu12
nvidia-cudnn-cu12

# 화면 OCR (PaddleOCR, CPU 전용)
paddlepaddle
paddleocr
