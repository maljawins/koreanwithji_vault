# 260911 작업 로그

> Fable 모델 세션에서 돌릴 vault 점검 + 1차 OUTLINE 관통 테스트 prompt 계획을 세운 날.
> 이 날 자체는 vault 파일을 거의 고치지 않았다 — 산출물은 vault 문서가 아니라 **다음 세션에서 Ji가 순서대로 붙여넣을 prompt 대본**이다.
> 점검 과정에서 vault를 실측해 심각한 문제 2건과 문서 정합성 문제 8건을 찾았다.

---

## 이 날의 작업 흐름

| 순서 | 작업 목적 | 작업 내용 | 결과 |
|---|---|---|---|
| 1 | Fable 세션용 점검+테스트 계획 수립 | 점검 prompt 3개 + 관통 테스트 prompt 7개로 구성된 계획서 작성, Ji 승인 | 완료 (계획, 실행은 다음 세션) |

---

## 계획 구성

**Part A · 점검 (prompt 3개)**: P1 부팅·라우팅 검증, P2 구조·수치 대조, P3 인프라·리스크

**Part B · 관통 테스트 (prompt 7개)**: P4 template 2종, P5 topic_copy, P6 조준 발췌+문자열 대조, P7 topic_shaped Ji 검수, P8 spec_done+concept_index 씨앗, P9 OUTLINE, P10 정답지 대조. 검증 항목 9개(V-1~V-9).

테스트 레슨은 `Level 2-01 은/는 & 이/가`로 정했다(처음엔 정답지 없는 L1-18 Plain Form이었다가 교체). 폴더 실측: 파일 26개 612KB, 트랙 4개(Ji_draft 1·yt_md 11·Amira 7·HTSK 7). 계획서 파일: `C:\Users\Donga\.claude\plans\fable-vault-graceful-marshmallow.md`

**왜 정답지 있는 레슨으로 바꿨나.** Ji 친작 draft 371줄이 있어, 테스트가 재는 것이 "발췌만으로 레슨이 만들어지는가"에서 **"Ji가 쓴 것이 spec을 거쳐 outline까지 오는 동안 얼마나 새는가(손실률)"** 로 바뀐다. `course_plan.md`가 최대 리스크로 꼽은 "Ji 색깔 흔들림"은 정답지가 있어야 잴 수 있으므로, 이 쪽이 그 리스크를 정면으로 시험한다. 이에 맞춰 P10(정답지 대조)을 새로 넣고 손실률·생성 단계 환각·톤 보존을 검증 항목에 추가했다. 발췌 전용 경로는 이 테스트로 덜 시험되므로 2차 테스트로 분리했다(이후 2026-09-13 개정으로, STEP 2에서 44개를 전부 발췌하며 자동으로 같이 시험되는 것으로 정리됨).

---

## 점검 단계에서 찾은 문제

### 심각 (라인 돌리기 전에 판단 필요)

| # | 발견 |
|---|---|
| R-1 | **vault 전체가 git untracked.** git은 옛 구조 1,619개를 삭제 상태로 추적 중(브랜치 `post-level2-cycle`). 2026-09-08에 이미 `to_do.md`에 리스크로 올라갔던 항목인데 아직 미처리 |
| R-2 | **`kor_yt_md_pipeline.py`가 실행되면 죽는다.** 105·106행이 없는 폴더 `01_Raw`를 가리키고(실제는 `02_product/01_raw`), 305행에서 존재 확인 없이 `load_workbook`을 호출한다. `kor_monitor_server.py`는 같은 상수지만 guard가 있어 죽지 않는다 |

### 문서 정합성 (D-1~D-8)

`master_index.md`가 실재 폴더·문서 누락, `260907_log.md` 0바이트+인덱스 행 없음, `korean_yt_md_guide.md`의 죽은 경로 3곳, `.obsidian/graph.json`의 죽은 필터 4개, `00_curriculum_and_index/`에 index .md 없음, 트랙명 `05_Reddit`과 파일명 `05_web_index.xlsx` 불일치 등 8건. 별건으로 **DP-1이 "Obsidian MCP + Skill"로 확정돼 있는데 vault에 `.mcp.json`이 없다** — 결정과 실물이 어긋나 있다.

---

## Ji 결정 대기로 올린 6건 (Q-A~Q-F)

Q-A 점검 후 즉시 수정 여부, Q-B git 정리 시점, Q-C template 저장 위치, Q-D outline 파일명 규칙, Q-E 파이프라인 경로 수정 시점, Q-F 발췌 전용 2차 테스트 시점. 각 항목에 권장안을 붙였고 무응답 시 권장안으로 간다고 명시했다.

---

## Stop 훅과 plan mode 충돌 발견

`stop_log_enforcer.py`(conversation_log.md 갱신 안 하면 턴 종료를 막는 Stop 훅)가 plan mode에서도 갱신을 요구해 턴이 2회 차단됐다. plan mode는 계획 파일 외 쓰기가 금지라 조건을 못 채운다. Ji와 상의해 "plan 승인 후 한 번에 기록"으로 운영하기로 하고, 훅 자체 수정은 별건으로 미조치 항목에 올렸다.

---

## 작성·갱신한 문서

**신설**: `C:\Users\Donga\.claude\plans\fable-vault-graceful-marshmallow.md` (계획서, vault 밖 경로)

**vault 내 수정**: 없음. 이 날의 산출물은 다음 Fable 세션에서 실행할 prompt 대본이다.

---

## 미해결로 남긴 것

- R-1(git 미추적), R-2(파이프라인 죽는 경로) — Ji 판단 대기, 2026-09-13 시점까지 미처리
- 문서 정합성 D-1~D-8 — 미처리
- DP-1과 실물(`.mcp.json` 없음) 불일치 — 미처리
- Q-A~Q-F 6건 — Ji 결정 대기
- Stop 훅 plan mode 예외 — 미조치 항목으로 이월

---

## 하루 끝 기준 상태

계획서 승인 완료, 실제 Fable 세션 실행은 아직. vault 파일 변경 없음. 2026-09-13 STEP 순서 개정 때 이 계획의 관통 테스트(L2-01)가 STEP 4 dry-run으로 재배치됐다.
